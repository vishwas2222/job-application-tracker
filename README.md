# Arnifi Job App (intern assignment)

## Deployment instructions

1. Build locally with Docker:
   - `docker compose build`
   - `docker compose up`

2. CI runs automatically in GitHub Actions.

3. For production, set `SECRET_KEY` to a secure value and use a real database.
